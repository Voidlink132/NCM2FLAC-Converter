// 项目根目录 build.gradle
plugins {
    // AGP 最新稳定版，已验证与Gradle版本完全兼容
    id 'com.android.application' version '8.4.0' apply false
    id 'com.android.library' version '8.4.0' apply false
}

// 全局任务配置
tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
